'use strict';

var grunt = require('grunt');

exports.iconfonts = {
  setUp: function(done) {
    // setup here if necessary
    done();
  },
  todo: function (test) {
    test.ok(true);
    test.done();
  }
};
